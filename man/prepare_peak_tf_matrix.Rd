% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_prepare_peak_TF_matrix.R
\name{prepare_peak_tf_matrix}
\alias{prepare_peak_tf_matrix}
\title{Prepare a binary peak-by-TF matrix}
\usage{
prepare_peak_tf_matrix(
  peak_motif_mat,
  da_peaks,
  motif_anno_file = NULL,
  filter_expr = FALSE,
  expr_tfs = NULL,
  min_sites = 5,
  min_percent = NULL,
  remove_motifs = NULL
)
}
\arguments{
\item{peak_motif_mat}{A data frame or matrix containing a \code{peakID} column and
motif columns. Non-zero motif entries are interpreted as motif presence.}

\item{da_peaks}{Character vector of peak IDs to retain.}

\item{motif_anno_file}{Optional path to a tab-delimited motif annotation file with
columns \code{motif_id}, \code{cluster}, and \code{tf_name}. If \code{NULL}, the bundled
\code{extdata/metadata.tsv} file is used.}

\item{filter_expr}{Logical. If \code{TRUE}, retain only TFs listed in \code{expr_tfs}.}

\item{expr_tfs}{Character vector of expressed TFs. Required when
\code{filter_expr = TRUE}.}

\item{min_sites}{Minimum number of peaks containing a TF motif when
\code{min_percent} is \code{NULL}.}

\item{min_percent}{Optional minimum fraction of peaks containing a TF motif.
When supplied, this criterion is used instead of \code{min_sites}.}

\item{remove_motifs}{Character vector of motif IDs to exclude. If \code{NULL},
\code{DEFAULT_REMOVE_MOTIFS} is used. Use \code{character(0)} to disable motif removal.}
}
\value{
A list with elements \code{X} (binary peak-by-TF numeric matrix), \code{Peaks},
\code{TFs}, \code{nPeaks}, \code{nTFs}, and \code{expressed_tfs}.
}
\description{
Convert a peak-by-motif matrix into the binary peak-by-TF matrix used by
PROACT. The function restricts the input to selected peaks, removes redundant
motifs, maps motif IDs to TF names, collapses multiple motifs for the same TF,
binarizes the matrix, and optionally filters TFs by binding frequency and
expression.
}
\details{
Motif columns not found in \code{motif_anno_file} are removed with a
warning. Binarization is applied after duplicated TF columns are collapsed,
ensuring that the final matrix contains only 0/1 values.
}
\examples{
\dontrun{
peak_tf <- prepare_peak_tf_matrix(
    peak_motif_mat = peak_motif_mat,
    da_peaks = da_peaks
)
}
}
