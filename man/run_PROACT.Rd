% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_run_PROACT.R
\name{run_PROACT}
\alias{run_PROACT}
\title{Run PROACT cooperative TF inference}
\usage{
run_PROACT(
  peak_tf_matrix,
  prior_mat,
  target_tfs,
  all_jaspar_tf = TRUE,
  return.pos = TRUE,
  motif_anno_file = NULL,
  remove_motifs = NULL,
  ncores = 4,
  block1.penalization = TRUE
)
}
\arguments{
\item{peak_tf_matrix}{Output from \code{\link[=prepare_peak_tf_matrix]{prepare_peak_tf_matrix()}}.}

\item{prior_mat}{Symmetric TF prior matrix, typically produced by
\code{\link[=build_prior]{build_prior()}}.}

\item{target_tfs}{Character vector of TFs to use as response TFs.}

\item{all_jaspar_tf}{Logical. If \code{TRUE}, all TFs shared by the peak-by-TF
matrix and prior matrix may be predictors. If \code{FALSE}, the prior matrix is
first restricted to \code{target_tfs}.}

\item{return.pos}{Logical. If \code{TRUE}, retain only associations with positive
LASSO coefficients after unordered-pair deduplication.}

\item{motif_anno_file}{Optional path to a motif annotation file with columns
\code{motif_id}, \code{cluster}, and \code{tf_name}. If \code{NULL}, the bundled
\code{extdata/metadata.tsv} file is used.}

\item{remove_motifs}{Character vector of motif IDs to exclude when defining
motif clusters. If \code{NULL}, \code{DEFAULT_REMOVE_MOTIFS} is used. Use
\code{character(0)} to disable motif removal.}

\item{ncores}{Number of parallel workers. A serial backend is used when
\code{ncores = 1}; socket parallelism is used on Windows and multicore
parallelism on Unix-like systems.}

\item{block1.penalization}{Logical passed to \code{\link[prioritylasso:prioritylasso]{prioritylasso::prioritylasso()}}
to determine whether the first priority block is penalized.}
}
\value{
A data frame of candidate TF pairs with columns including \code{TF1},
\code{TF2}, \code{beta}, \code{Cluster1}, \code{Cluster2}, \code{prior}, and \code{pairs}.
}
\description{
Fit prior-regularized priority LASSO models to a binary peak-by-TF matrix and
infer candidate cooperative TF pairs. For each target TF, motif occupancy is
modeled as the response and the remaining TFs are used as predictors, with
predictor blocks defined by the prior matrix.
}
\details{
TFs belonging to the same motif cluster as the response TF are
removed from its predictor set. When the same unordered TF pair is inferred
in both directions, only the result with the largest absolute coefficient
is retained. A positive coefficient represents a positive conditional motif
association and should not by itself be interpreted as direct physical
interaction or causal regulation.
}
\examples{
\dontrun{
pairs <- run_PROACT(
    peak_tf_matrix = peak_tf,
    prior_mat = prior_mat,
    target_tfs = c("JUN", "FOS"),
    ncores = 4
)
}
}
