% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_build_prior.R
\name{build_prior}
\alias{build_prior}
\title{Build a TF prior matrix}
\usage{
build_prior(jaspar_tfs, ppi_dir)
}
\arguments{
\item{jaspar_tfs}{Character vector of TF names to include in the prior matrix.}

\item{ppi_dir}{Directory containing \code{BioGRID.tsv}, \code{STRING.tsv},
\code{RF2PPI.tsv}, and \code{CAPSELEX.tsv}.}
}
\value{
A symmetric numeric matrix with TF names as row and column names.
Values are 1 for high-priority prior pairs, 2 for lower-priority prior
pairs, and 0 when no prior evidence is assigned.
}
\description{
Construct a symmetric transcription factor (TF) prior matrix from BioGRID,
STRING, RF2PPI, and CAP-SELEX evidence. TF pairs supported by at least two
sources are assigned priority 1, pairs supported by one source are assigned
priority 2, and all remaining pairs are assigned 0.
}
\details{
CAP-SELEX provides DNA-guided TF-pair evidence and is treated as
prior evidence rather than direct proof of a physical interaction in vivo.
The returned matrix is used to define predictor blocks in \code{\link[=run_PROACT]{run_PROACT()}}.
}
\examples{
\dontrun{
tfs <- c("JUN", "FOS", "CEBPB")
prior_mat <- build_prior(tfs, ppi_dir = "PPI_data/human")
}
}
