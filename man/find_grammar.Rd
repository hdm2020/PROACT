% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_get_coTF_grammar.R
\name{find_grammar}
\alias{find_grammar}
\title{Identify spatial grammar for TF pairs}
\usage{
find_grammar(
  object = NULL,
  features = NULL,
  pairs,
  assay = NULL,
  motif_gr_list = NULL,
  strategy = c("best", "nearest"),
  motif_anno_file = NULL,
  remove_motifs = NULL,
  region_length = NULL,
  genome = NULL
)
}
\arguments{
\item{object}{Optional Signac/Seurat object whose selected assay contains motif
positions. Used for single-cell chromatin accessibility data.}

\item{features}{Character vector of genomic regions formatted as
\code{chr-start-end}. Required when \code{object} is supplied.}

\item{pairs}{Character vector of TF pairs formatted as \code{TF1_TF2}.}

\item{assay}{Name of the assay containing motif positions. Required when
\code{object} is supplied.}

\item{motif_gr_list}{Optional named GRangesList containing motif positions
for bulk data. List names should be motif IDs, and each motif instance
must contain \code{peak_id} and \code{score} metadata columns.}

\item{strategy}{Pair-selection strategy within each peak: \code{"best"} selects
the highest-scoring motif instance for each TF; \code{"nearest"} selects the
motif pair with the smallest center-to-center distance.}

\item{motif_anno_file}{Optional path to the motif annotation file with columns
\code{motif_id}, \code{cluster}, and \code{tf_name}. If \code{NULL}, the bundled
\code{extdata/metadata.tsv} file is used.}

\item{remove_motifs}{Character vector of motif IDs to exclude. If \code{NULL},
\code{DEFAULT_REMOVE_MOTIFS} is used. Use \code{character(0)} to disable removal.}

\item{region_length}{Optional width in base pairs for resizing each feature
around its center before motif overlap analysis.}

\item{genome}{Genome object providing chromosome lengths. Required when
\code{region_length} is not \code{NULL}.}
}
\value{
A named list with one element per requested TF pair. Each non-empty
element is a data frame describing motif spacing, strand combination,
geometry, arrangement, order, and motif scores for representative instances
across peaks.
}
\description{
Extract motif instances for candidate TF pairs within selected chromatin
regions and summarize their relative spacing, strand geometry, and order.
}
\details{
\code{edge_distance} is defined as the downstream motif start minus the
upstream motif end. \code{center_distance} is the distance between motif centers.
Geometry is classified as \code{HH}, \code{TT}, or \code{HT}; \code{HT} cases are further split
into \verb{A,B_HT} and \verb{B,A_HT} according to motif order.
}
\examples{
\dontrun{
grammar <- find_grammar(
    object = atac,
    features = da_peaks,
    pairs = c("JUN_FOS", "CEBPB_ATF4"),
    assay = "peaks",
    genome = genome
)
}
}
