% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_plot_coTF_network.R
\name{plot_PROACT_network}
\alias{plot_PROACT_network}
\title{Plot a PROACT TF-TF network}
\usage{
plot_PROACT_network(pair_candidates, anchor_tfs = NULL)
}
\arguments{
\item{pair_candidates}{A data frame containing \code{TF1}, \code{TF2}, and \code{beta}.
Output from \code{\link[=run_PROACT]{run_PROACT()}} can be supplied directly because its \code{prior}
column is converted to edge categories automatically. Alternatively, a
pre-existing \code{Category} column may be supplied.}

\item{anchor_tfs}{Optional character vector of anchor TFs to highlight.}
}
\value{
A ggraph/ggplot object representing the TF association network.
}
\description{
Visualize PROACT candidate TF pairs as an undirected network. When anchor TFs
are supplied, partner TFs are assigned to the anchor with the largest pair
coefficient. Without anchors, Louvain community detection defines network
modules.
}
\details{
When \code{Category} is absent, \code{prior > 0} is labeled
\code{"Prior-supported"} and \code{prior == 0} is labeled \code{"No prior"}. Existing
\code{"Known PPI"} and \code{"Unknown PPI"} categories remain supported for backward
compatibility.
}
\examples{
\dontrun{
p <- plot_PROACT_network(pair_candidates, anchor_tfs = c("JUN", "CEBPB"))
p
}
}
